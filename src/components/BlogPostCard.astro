---
interface Props {
  title: string;
  date: string;
  excerpt: string;
  slug: string;
}

const { title, date, excerpt, slug } = Astro.props;

const formattedDate = new Date(date).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<a
  href={`/blog/${slug}`}
  class="group flex flex-col gap-2 py-6 border-b border-neutral-200 last:border-0 hover:opacity-80 transition-opacity"
>
  <time class="text-xs text-neutral-400 uppercase tracking-wide">{formattedDate}</time>
  <h3 class="text-base font-semibold text-neutral-900 group-hover:underline underline-offset-2">
    {title}
  </h3>
  <p class="text-sm text-neutral-500 leading-relaxed line-clamp-2">
    {excerpt}
  </p>
</a>
